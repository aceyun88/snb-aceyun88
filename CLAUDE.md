# 강사 랜딩페이지 — 프로젝트 규칙

> **세션은 D:\Projects\snb-k-bridge 폴더에서 연다(2026-09-05 확정).** 이 저장소만 고치는 일감도 브릿지 폴더에서 연 세션으로 한다. 지침(CLAUDE.md 3절)·계획서·배포 플레이북·비용 관리표·실수 기록·메모리가 거기에 있고, 세 저장소 이름 점검(scripts/check-names.sh)도 거기서 돈다. 다른 폴더에서 열렸으면 먼저 알린다.

윤성임(AI융합교육전문가 · 소셜앤비즈 대표)의 강의 섭외용 랜딩페이지다.
방문자가 강사를 신뢰하고 **출강 요청서를 작성하게 하는 것**이 유일한 목적이다.

## 대화 규칙

- 모든 응답은 경어(존댓말)로 쓴다.
- 문장을 콜론(`:`)으로 끝내지 않는다.
- 불확실하면 추측하지 말고 먼저 묻는다.
- 검증되지 않은 통계·수치를 쓰지 않는다.

## 파일 구조

```
index.html                 화면 전체. CSS까지 이 파일 안에 있다
profile-yun-sung-im.jpg    프로필 사진 (874x912, 50KB)
404.html                   없는 쪽 안내
netlify.toml               배포 설정과 내부 문서 차단 규칙
CLAUDE.md                  이 문서 (루트에 있어야 Claude Code가 읽는다)
docs/                      운영 문서. 사이트로 공개되지 않는다
```

**한 장짜리 사이트다.** 자바스크립트가 없다. 빌드 도구도 없다.
이 단순함이 이 사이트의 가장 큰 자산이다. 늘리지 않는다.

## 브랜드 규격

| 항목 | 값 |
|---|---|
| 네이비 (기본) | `#061b3a` |
| 블루 (보조) | `#0b376d` |
| 골드 (강조·단추) | `#c9922e` |
| **골드 잉크 (밝은 바탕 위 글씨)** | `#8a6414` |
| 본문 | `#172033` |
| 회색 글씨 | `#667085` |
| 밝은 바탕 | `#f6f8fb` |
| 모서리 반경 | 단추 999px, 상자 18~32px |

- **밝은 바탕 위 금색 글씨에는 `--gold-ink`를 쓴다.** `--gold`를 쓰면 흐려서 안 읽힌다
- 넓은 면·단추 바탕에는 `--gold`를 쓴다

프롬프트 팩 사이트(`snb-prompt-pack`)와 색이 다르다. **일부러 다르다.**
랜딩은 둥글고 부드럽게, 팩은 각지고 서식처럼 간다.

## 호칭과 수치 표기

**프롬프트 팩 저장소의 `CLAUDE.md`와 반드시 같아야 한다.**
두 사이트가 서로 링크로 이어져 있어, 오가는 사람이 다른 숫자를 보면 안 된다.

- 호칭은 **AI융합교육전문가**로 쓴다
- **쓸 수 있는 값** — 기술창업학 박사 / KT·KTDS 약 16년 / 저서·공저 50여 종
- **쓰지 않는 값** — 누적 출강 횟수 / 저서 36권·51종 / "약 30년 디지털 현장"
- **학력** — 동국대 기술창업학 박사 / 한성대 컨설팅학 석사 / 서울시립대 전산통계학 이학사
- **이력에서 빼는 것** — 디지털융합교육원 부원장
- 연락 이메일은 **aceyun88@gmail.com** 하나로 쓴다(2026-09-06 naver→gmail 통일, naver는 gmail로 자동 전달)

## 용어 규칙

- 이 사이트에서는 **프롬프트**라고 부른다. 차림표도 「무료 프롬프트」다
- 프롬프트 팩 사이트에서는 **지시서**라고 부른다. 그쪽이 그 사이트의 정체성이다
- 두 낱말을 잇는 자리(무료 프롬프트 안내 구획)에서만 `지시서(프롬프트)`로 병기한다

**왜 다르게 부르나** — 랜딩에 온 사람은 "지시서"를 모른다. "프롬프트"가 익숙하다.
팩에 들어가면 그곳의 이름을 배운다.

## 문의 창구

**강의 문의는 구글 폼 하나로만 받는다.**

- https://forms.gle/zseFvFP8LaFWBUG29 (「윤성임 강사의 출강 요청 접수」)
- 사이트 안에 입력 폼을 만들지 않는다. 창구가 둘이면 하나를 놓친다
- 전화·이메일 직접 연락은 그대로 둔다

> 예전에 사이트 안에 신청 폼이 있었는데 설정값이 자리표시자인 채로 돌아가
> **"접수되었습니다"만 뜨고 어디에도 도착하지 않았다.** 그래서 걷어냈다.

## 기술 제약

- 자바스크립트를 넣지 않는다. 지금 한 줄도 없다
- 외부 스크립트·글꼴 CDN을 쓰지 않는다. 시스템 글꼴로 충분하다
- `localStorage`·`sessionStorage`를 쓰지 않는다
- 빌드 도구를 넣지 않는다
- 새 문서를 `docs/` 밖에 두면 `netlify.toml`에 차단 규칙을 더한다

## 글자 대비

**밝은 바탕 위 글씨는 4.5:1 이상이어야 한다.** 큰 글씨(24px 이상 또는
18.66px 이상 굵게)는 3:1 이상.

고치고 나면 `docs/02-운영매뉴얼.md`의 대비 검사 명령을 돌려 확인한다.

## 연결된 것

| 무엇 | 어디 |
|---|---|
| 프롬프트 팩 | https://snb-prompt-pack.netlify.app |
| 강의 문의 폼 | https://forms.gle/zseFvFP8LaFWBUG29 |
| 소스 저장소 | github.com/aceyun88/snb-aceyun88 |
| Netlify site-id | `621da9e8-8084-437a-a1f2-7df20c034903` |

## 작업 방식

- 실제로 두 번 이상 반복된 규칙만 이 문서에 반영한다
- 큰 변경 전에는 먼저 계획을 제시하고 승인을 받는다
- 고친 뒤에는 폭 375·768·1440에서 가로 넘침과 글자 대비를 확인한다
